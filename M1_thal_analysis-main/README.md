# M1_thal_analysis

Data can be found here: https://drive.google.com/drive/folders/13jVg3Q-MXMWkfFu2eGVVKZe3ReDM4zmU
Prior to working with data, run `python preprocess/create_task_events.py` and `python preprocess/epoch_data.py`. Note that once data is downloaded from the link above, each folder may already contain `taskevents.pkl`, but it may be outdated. So re-running the preprocessing scripts is recommended.